---
directories:
  forrest:
    url: 'https://raw.githubusercontent.com/startwind/forrest-directory/main/directory.yml'
  360monitoring:
    loader:
      type: github
      config:
        repository: forrest-directory
        user: leankoala-gmbh
        file: directory.yml
        token: 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

repositories:
  # "friends-of-wp":
  #  adapter: gist
  #  name: "Friends of WP"
  #  description: "Some very helpful WordPress commands."
  #  config:
  #    username: "friends-of-wp"
  #    prefix: 'wpdev: '

  forrest:
    type: api
    official: true
    name: "Forrest Big Database"
    description: "The big database with all commands known by Forrest."
    config:
      endpoint: https://forrest.startwind.io/
